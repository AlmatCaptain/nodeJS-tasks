<%- include('includes/head.ejs') %>

		<% if (users.length > 0) { %>
		    <ul>
			    <% for (let usr of users) { %>
                    <li><%= usr.name %></li>
                <% } %>
            </ul>
        <% } else { %>
            <h1>No users found!</h1>
        <% } %>
	</body>
</html>
